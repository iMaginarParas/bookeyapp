name: bookey
description: Transform your ideas into stunning videos
publish_to: 'none'

version: 1.0.0+3

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^8.0.0+1
  http: ^1.2.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1
  video_player: ^2.8.1
  chewie: ^1.7.4
  purchases_flutter: ^6.28.1  # ← ADDED: RevenueCat for subscriptions and purchases

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/icon.png